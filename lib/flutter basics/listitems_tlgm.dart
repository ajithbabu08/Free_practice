import 'package:flutter/material.dart';

class TelegramList extends StatelessWidget {
  const TelegramList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        // bottom:  
      ),
    );
  }
}